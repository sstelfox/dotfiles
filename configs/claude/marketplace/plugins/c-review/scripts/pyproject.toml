[project]
name = "c-review-scripts"
version = "0.1.0"
description = "Build-plan + SARIF helpers for the c-review plugin"
requires-python = ">=3.13"
dependencies = []

[tool.ruff]
line-length = 100

[tool.ty.rules]
