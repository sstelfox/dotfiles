#!/usr/bin/env false

if [ -f /etc/zshrc ]; then
  source /etc/zshrc
fi

export __DOTFILE_ROOT="${HOME}/.dotfiles"

# Needed by other sourced inclusions, provides __plc and other functions
source "${__SHELL_CFG_ROOT}/common/personal_posix_lib.sh.inc"

source "${__SHELL_CFG_ROOT}/common/dotfiles_path.sh.inc"
source "${__SHELL_CFG_ROOT}/common/telemetry_opt_out.sh.inc"
source "${__SHELL_CFG_ROOT}/common/git_aliases.sh.inc"
