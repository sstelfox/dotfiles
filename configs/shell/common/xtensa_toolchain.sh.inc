#!/usr/bin/env false

#if [ "${ENABLE_XTENSA:-}" = "true" ] && [ -f "/opt/esp-idf/export.sh" ]; then
#  if [ "${XTENSA_LOADED}" != "true" ]; then
#    source /opt/esp-idf/export.sh
#    export XTENSA_LOADED="true"
#  fi
#fi
